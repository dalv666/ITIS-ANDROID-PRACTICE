<?xml version="1.0" encoding="utf-8"?>
<manifest package="org.dalv.practice.android.itis.notification"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

        <receiver android:name=".AlarmReceiver">
            <intent-filter>
                <action android:name="org.dalv.practice.android.itis.notification.MainActivity.ON_ALARM_ACTION"/>
                <action android:name="org.dalv.practice.android.itis.notification.AlarmReceiver.ON_APP_CLOSE"/>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="org.dalv.practice.android.itis.notification.AlarmReceiver.ON_NOTIFICATION_CLOSE"/>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE"/>
                <action android:name="android.intent.action.BATTERY_LOW"/>
                <action android:name="org.dalv.practice.android.itis.notification.AlarmReceiver.ON_WIFI_NAME_PASS"/>
            </intent-filter>
        </receiver>
    </application>

</manifest>
